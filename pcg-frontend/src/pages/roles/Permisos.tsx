import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const Permisos = () => {
  return (
    <Container>
      <Box sx={{ mt: 4 }}>
        <Typography variant="h4" component="h1" gutterBottom>
          Gestión de Permisos
        </Typography>
        {/* Agregar funcionalidad de gestión de permisos aquí */}
      </Box>
    </Container>
  );
};

export default Permisos; 