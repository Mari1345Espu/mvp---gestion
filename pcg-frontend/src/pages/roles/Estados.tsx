import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const Estados = () => {
  return (
    <Container>
      <Box sx={{ mt: 4 }}>
        <Typography variant="h4" component="h1" gutterBottom>
          Gestión de Estados
        </Typography>
        {/* Agregar funcionalidad de gestión de estados aquí */}
      </Box>
    </Container>
  );
};

export default Estados; 