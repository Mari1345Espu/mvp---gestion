import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const Usuarios = () => {
  return (
    <Container>
      <Box sx={{ mt: 4 }}>
        <Typography variant="h4" component="h1" gutterBottom>
          Gestión de Usuarios
        </Typography>
        {/* Agregar funcionalidad de gestión de usuarios aquí */}
      </Box>
    </Container>
  );
};

export default Usuarios; 